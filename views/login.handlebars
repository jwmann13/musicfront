<div class="container">
    {{#if messages.error}}
    {{messages.error}}
    {{/if}}
    <div class="row">
        <div class="col-3"></div>

        <div class="card col-6">
            <h1 class="display-4">Login</h1>
            <!-- Login Form -->
            <form action="/login" method="POST" id="loginForm">
                <div class="form-group">
                    <label for="login">Username</label>
                    <input type="text" id="login" class="" name="login" placeholder="login" required>
                </div>
                <div class="form-group">
                    <label for="password">Password</label>
                    <input type="text" id="password" class="" name="password" placeholder="password" required>
                </div>
                <div class="form-group">
                    <input type="submit" class="btn btn-primary" value="Log In">
                </div>
            </form>

            <!-- Remind Passowrd -->
            <div id="formFooter">
                <a class="underlineHover" href="#">Forgot Password?</a>
            </div>
            <div>
                <a href="/register">Not a member? Register a new account!</a>
            </div>

        </div>

        <div class="col-3"></div>

    </div>
</div>